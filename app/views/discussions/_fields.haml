.field
  = f.label :name
  = f.text_field :name
  = render 'shared/field_errors', f: f, field: :name
.field
  = f.label :body
  = f.text_area :body
  = render 'shared/field_errors', f: f, field: :body
